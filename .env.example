# PostgreSQL connection
# Use DATABASE_HOST=postgres when the API runs inside docker-compose.dev.yml
# Use DATABASE_HOST=localhost when the API runs on the host machine
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=root
DATABASE_NAME=pdp-api
